# Sector Vault Project
内容去中心化储存的基于Git的无头CMS，使用Astro开发。

## 主站点
1. 支持不同git平台部署（GitHub、Atomgit、Gitee）。
2. 支持OAuth（GitHub Device Flow）或PAT（GitHub App）授权认证。
3. 使用@gitee/typescript-sdk-v5实现giteeAdapter。
4. 使用octokit实现githubAdapter。
5. 与索引数据共仓库，通过不同分支管理。
6. i18n支持，默认支持简体中文、繁体中文、英语、日语。

前端路由：
```
/ -- 首页
/project -- 投稿列表
/article -- 专栏列表
/discussions -- 讨论
/faq -- 常见问题
/about -- 站点介绍
/user/:name -- 用户空间
/collection/:name/:repo -- 集合详情
/view/:name/:repo/:slug -- 详情页
/new -- 新建投稿
/edit/:name/:repo/:slug -- 编辑稿件
```

### 投稿列表
1. 从索引仓加载，分页展示最近的投稿，默认展示10条。
2. 点击下一页时，如果默认索引显示完毕，则自动加载已归档的索引，直至遍历完所有索引。加载期间在列表最下面显示加载中图标和取消按钮。
3. 提供筛选器，可以根据关联曲目、合成引擎、使用声库、歌曲语言筛选。
4. 提供搜索器，输入关键字点搜索按钮后，遍历索引，列出关联稿件。
5. 列表显示稿件封面、标题、投稿时间、用户名、关联曲目、合成引擎、使用声库、歌曲语言、有无参数。
6. 自动获取当前页的稿件关联的issue和release，获取评论数和点赞数，并缓存。

### 投稿详情
1. 页面开始展示投稿标题和时间，以列表形式显示关联曲目、合成引擎、使用声库、歌曲语言、有无参数。
2. 然后渲染正文，不用包含文件头的各种属性。
3. 正文结束后以横排按钮形式显示tag列表。
4. 然后显示仓库内媒体，图片提供预览，音频、视频提供播放组件。
5. 显示投稿用户名、头像、仓库名，仓库收藏（star）数，和当前稿件许可证名称，并能够点击查看跳转；当前稿件没有指定许可证时显示仓库的许可证。
6. 然后列出工程文件，如果有加密提供密码输入框，点击下载在前端先加载完整个文件，先解压解密再返回浏览器文件。
7. 嵌入稿件关联的release并展示互动记录和附件列表。
8. 嵌入稿件关联的issue列表当作留言。

### 专栏列表
1. 基本与投稿列表一致，但不提供筛选器。
2. 稿件只显示标题、投稿时间、用户名，如果有封面或任意图片媒体也显示。

### 专栏详情
1. 页面开始展示投稿标题、时间和用户名、头像、仓库名。
2. 然后渲染正文，不用包含文件头的各种属性。
3. 嵌入稿件关联的issue列表当作留言。

### 讨论
1. 显示主站点仓库的discussions页面作为讨论区。

### 场景问题
1. 提供页面显示主站点仓库的wiki页面作为FAQ。

### 站点介绍
1. 介绍本网站架构和优势。
2. 显示索引的稿件数量和用户数量。

## 用户空间
用户空间既作为主站点的一个子路由，也可以独立部署；用户可自行部署用户空间作为内容仓的静态页面，只展示当前仓库的数据。

### 个人首页
1. 显示用户名头像。主站点部署时，如果当前用户索引包含静态部署页面，提供按钮跳转。
2. 主站点部署时，展示用户账户下所有固定前缀`svp-`的仓库作为合集，并显示每个仓库最近的3个工程稿件封面标题日期和更多按钮，合集之间水平分隔。显示仓库收藏（star）数量。
3. 将包含专栏投稿的仓库再显示一遍，显示最近的3个专栏稿件和更多按钮，合集之间水平分隔。
4. 工程和专栏合集有大标题分隔。
5. 如果某个仓库包含个人介绍文件（`ABOUT.md`），展示第一个找到的内容，

### 集合详情
1. 个人页点击集合更多按钮，或投稿详情页点击仓库名进入集合详情页。
2. 显示当前仓库名、用户名和头像、仓库许可证。显示投稿按钮。
3. 工程和专栏各一个标签按钮，切换显示。
4. 工程标签分页展示当前仓库内所有工程稿件，默认10稿每页，显示封面标题日期，稿件上显示编辑和删除按钮。
5. 自动获取当前页的稿件关联的issue和release，获取评论数和点赞数，并缓存。
6. 专栏标签分页展示当前仓库内所有专栏稿件。

### 编辑器
1. 登录选择数据托管平台。
2. 创建投稿合集自动从模板仓库创建固定前缀内容仓到用户账户；也支持创建空仓库写入。
3. 提供仓库级许可证修改，提供CC0、CC4.0（含细分系列）可选，支持自行输入许可证。投稿支持选择目录级许可证。
4. 唯一字段：标题、简介、备注、有无参数；支持多值字段：标签、视频站链接、关联曲目、合成引擎、使用声库、歌曲语言；
5. 允许上传多个工程文件；每个文件选框提供多个单选框，控制上传方案，默认选择格式化。
   1. 不处理: 将文件原始字节上传到仓库。
   2. 格式化: 选中且文件内容是json或xml格式则将文件折行缩进再上传；
   3. 压缩: 使用gzip将文件压缩一遍再上传；
   4. 加密: 额外显示密码输入框，使用AES256加密文件后上传。
6. 支持投稿创建关联仓库release和issue，支持修改投稿时上传附件到release并展示。
7. 投稿提交到用户仓库，同时向索引仓提交一个轻量pr，仅包含用户和投稿索引更新行。可选提交到当前平台的索引仓的镜像，或跨平台提交到选定仓库。
8. 增加投稿支持专栏类型，专栏不需要填写关联曲目、合成引擎、使用声库、歌曲语言、有无参数。

## 主站点-索引仓
1. 只包含一系列json和必要的配置文件。
2. 包含投稿用户和稿件索引；用户索引记录用户所在平台、投稿的仓库、静态页面链接，每个仓库一条记录；稿件索引记录每个稿件的投稿用户、标题、封面链接、投稿时间、关联曲目、合成引擎、使用声库、歌曲语言、有无参数。
3. 索引按照月份归档，未归档的索引包含最近1024条投稿；由定时任务触发归档。
4. 定时任务检查用户索引里的仓库是否存在，不存在则应删除
5. 索引分支允许所有人提交pr，门禁校验格式通过则自动合入。

## 内容仓
1. 内容仓库结构兼容DecapCMS。
2. 支持用户自行部署静态页面，链接写入用户索引。
3. 提供与主站点风格一致的模板仓库，默认支持浏览当前账户的所有仓库稿件。
4. 投稿包含固定`*Powered by Sector Vault Project*`，可以被git平台搜索引擎发现。
5. 生成RSS订阅
6. 投稿关联release
