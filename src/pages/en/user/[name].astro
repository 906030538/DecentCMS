---
import PageRaw from '@/pages/user/[name].astro';
const Page = PageRaw as any;

export function getStaticPaths() {
  return import.meta.env.DEV ? [{ params: { name: 'demo' } }] : [];
}
---

<Page />
