---
import Page from '@/pages/discussions.astro';
import { NON_DEFAULT_LOCALES } from '@/i18n';

export function getStaticPaths() {
  return NON_DEFAULT_LOCALES.map((locale) => ({ params: { locale } }));
}
---

<Page />
